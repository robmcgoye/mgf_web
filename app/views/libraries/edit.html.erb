<% provide(:title, t("library.edit")) %>
<div class="card mb-3 shadow-lg rounded border main-display mgf-body">
  <div class="card-body text-dark">
    <h2><%= t("library.edit") %></h2>
    <%= render 'form', library: @library %>
    <div class="btn-group p-2">
      <%= link_to t(:delete_button), @library, class: "btn btn-sm btn-outline-dark", 
                            method: :delete, data: { confirm: t(:delete_confirmation) } %>
      <%= link_to t(:view_button), @library, class: "btn btn-sm btn-outline-dark" %> 
      <%= link_to t(:back_button), libraries_path, class: "btn btn-sm btn-outline-dark"%>
    </div>
  </div>
</div>
