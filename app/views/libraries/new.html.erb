<% provide(:title, t("library.new")) %>

<div class="card mb-3 shadow-lg rounded border main-display mgf-body">
  <div class="card-body text-dark">
    <h2><%= t("library.new") %></h2>
    <%= render 'form', library: @library %>
    <%= link_to t(:back_button), libraries_path %>
  </div>
</div>

